/* const banners = require("../database/models/banners");
function bannerdata(req, res, next) {
    banners.find({})
    .then(banner => res.render('index', {banner}))
    .catch(next);
   
}
module.exports = {bannerdata} */
